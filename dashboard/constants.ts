// Constants and initial mock data (will be replaced by API calls)
import { Agent, Session, BotSettings, Webhook } from './types';

// These will be replaced by API calls in App.tsx
export const INITIAL_AGENTS: Agent[] = [];
export const INITIAL_SETTINGS: Partial<BotSettings> = {};
export const MOCK_SESSIONS: Session[] = [];
export const MOCK_WEBHOOKS: Webhook[] = [];
